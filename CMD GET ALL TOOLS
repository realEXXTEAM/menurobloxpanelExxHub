local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Prefix = "!Tls"

LocalPlayer.Chatted:Connect(function(message)
    if message:lower() == Prefix:lower() then
        -- Load FlyTools
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/Ygvn7l4R/raw"))()
            end)
        end)

        -- Load TpClick Tools
        task.spawn(function()
            pcall(function()
                loadstring(game:HttpGet("https://pastefy.app/K7iWHVMO/raw"))()
            end)
        end)
    end
end)
